import React from 'react';
import Header from '../header';
import Home from '../home';

const App = () => {
    /* console.log(props); */
    return (
        <div>
            <Header />
            <Home />
        </div>
    );
};

export default App;